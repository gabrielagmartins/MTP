#include <stdio.h>
#define QTDEMAX 1000
struct CAD {
    int idade;
};
int main() {
    int opc, i, qtde =0;
    struct CAD cadastro(QTDEMAX);
    while(1) {
        printf("1 - cadastra, 2 - sair")
        scanf("%d, &opc);
        if (opc == 2) break;
        cadastro[qtde].idade = 3;
        qtde++;
}
for()
return 0;
}


